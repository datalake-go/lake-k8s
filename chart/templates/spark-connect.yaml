apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "lakeorm.fullname" . }}-spark-connect
  labels:
    {{- include "lakeorm.labels" . | nindent 4 }}
    app.kubernetes.io/component: spark-connect
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "lakeorm.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: spark-connect
  template:
    metadata:
      labels:
        {{- include "lakeorm.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: spark-connect
    spec:
      {{- if .Values.serviceAccount.create }}
      serviceAccountName: {{ include "lakeorm.serviceAccountName" . }}
      {{- end }}
      containers:
        - name: spark-connect
          image: "{{ .Values.sparkConnect.image.repository }}:{{ .Values.sparkConnect.image.tag }}"
          imagePullPolicy: {{ .Values.sparkConnect.image.pullPolicy }}
          ports:
            - containerPort: 15002
              name: grpc
            - containerPort: {{ .Values.sparkConnect.service.uiPort | default 4040 }}
              name: http-ui
          resources:
            {{- toYaml .Values.sparkConnect.resources | nindent 12 }}
          env:
            - name: SPARK_CONNECT_CONF
              value: |-
                {{- range $k, $v := .Values.sparkConnect.conf }}
                --conf {{ $k }}={{ $v }}
                {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "lakeorm.fullname" . }}-spark-connect
  labels:
    {{- include "lakeorm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.sparkConnect.service.type }}
  ports:
    - name: grpc
      port: {{ .Values.sparkConnect.service.port }}
      targetPort: 15002
    - name: http-ui
      port: {{ .Values.sparkConnect.service.uiPort | default 4040 }}
      targetPort: http-ui
  selector:
    {{- include "lakeorm.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: spark-connect
